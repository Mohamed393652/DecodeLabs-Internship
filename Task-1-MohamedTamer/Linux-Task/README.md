# DecodeLabs Internship - Week 1

## Linux Task

This task demonstrates basic Linux file and directory management operations.

### Tasks Completed

* Created the `/app/logs` directory.
* Created the `/app/config.conf` configuration file.
* Created a server log file containing `Started`.
* Verified the `/app` directory structure.
* Backed up the server log as `server.bak`.
* Audited the configuration file using `ls -l`.

### Project Structure

```text
Linux-Task/
├── app/
│   ├── config.conf
│   └── logs/
│       └── server.bak
├── README.md
└── screenshot.png
```

### Verification

The task was completed and tested in Ubuntu/WSL.

The included screenshot shows the commands used and their output as evidence of successful completion.
